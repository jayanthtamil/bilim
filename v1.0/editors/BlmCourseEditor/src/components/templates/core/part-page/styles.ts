import { makeStyles, createStyles } from "@material-ui/core/styles";

export const usePartPageTemplateStyle = makeStyles(
  () =>
    createStyles({
      root: {},
    }),
  { name: "BlmPartPageTemplate" }
);
