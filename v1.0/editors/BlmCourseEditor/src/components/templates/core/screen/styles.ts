import { makeStyles, createStyles } from "@material-ui/core/styles";

export const useScreenTemplateStyle = makeStyles(
  () =>
    createStyles({
      root: {},
    }),
  { name: "BlmScreenTemplate" }
);
