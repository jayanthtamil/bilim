import { makeStyles, createStyles } from "@material-ui/core/styles";

export const useQuestionTemplateStyle = makeStyles(
  () =>
    createStyles({
      root: {},
    }),
  { name: "BlmQuestionTemplate" }
);
